import React from "react";
import styled from "styled-components";

const Navigation = () => {
  return <Wrapper className="navigation_wrapper">Navigator</Wrapper>;
};

const Wrapper = styled.div`
  &.navigation_wrapper {
  }
`;

export default Navigation;
